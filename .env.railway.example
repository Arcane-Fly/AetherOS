# Railway Environment Configuration
# Copy this to Railway environment variables for each service

# Node Environment (Production for Railway)
NODE_ENV=production

# Database Configuration (Railway PostgreSQL addon provides DATABASE_URL automatically)
# Use ${{Postgres.DATABASE_URL}} in Railway variable references
# DATABASE_URL will be automatically set by Railway PostgreSQL addon

# JWT Configuration (Generate secure random string for production)
JWT_SECRET=your-super-secure-production-jwt-secret-min-32-characters

# Redis Configuration (Railway Redis addon provides REDIS_URL automatically)  
# Use ${{Redis.REDIS_URL}} in Railway variable references
# REDIS_URL will be automatically set by Railway Redis addon

# OpenAI Configuration (Required)
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI Model Configuration (following MODELS_MANIFEST.md)
OPENAI_DEFAULT_MODEL=gpt-4o-mini
OPENAI_CODE_MODEL=gpt-4o-mini
OPENAI_API_MODEL=gpt-4o
OPENAI_UI_MODEL=gpt-4o
OPENAI_CLI_MODEL=gpt-4o-mini

# OAuth Configuration (Update with your production OAuth credentials)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}/api/auth/google/callback

GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_CALLBACK_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}/api/auth/github/callback

# Session Configuration (Generate secure random string)
SESSION_SECRET=your-session-secret-production-min-32-characters

# CORS Configuration (Railway service references)
CORS_ORIGIN=https://${{frontend.RAILWAY_PUBLIC_DOMAIN}}
FRONTEND_URL=https://${{frontend.RAILWAY_PUBLIC_DOMAIN}}

# Logging Configuration
LOG_LEVEL=info

# Service Communication (Use Railway internal networking)
AUTH_SERVICE_URL=https://${{auth-service.RAILWAY_PRIVATE_DOMAIN}}
GENERATION_SERVICE_URL=https://${{generation-service.RAILWAY_PRIVATE_DOMAIN}}  
WEBSOCKET_SERVICE_URL=https://${{websocket-service.RAILWAY_PRIVATE_DOMAIN}}

# Frontend API Configuration
REACT_APP_API_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}/api
REACT_APP_WEBSOCKET_URL=https://${{websocket-service.RAILWAY_PUBLIC_DOMAIN}}

# Railway automatically provides:
# PORT - The port your service should bind to (NEVER set this manually)
# RAILWAY_ENVIRONMENT - Current environment (production/staging)
# RAILWAY_SERVICE_NAME - Name of the current service  
# RAILWAY_PUBLIC_DOMAIN - Public domain for the service
# RAILWAY_PRIVATE_DOMAIN - Private internal domain for service-to-service communication